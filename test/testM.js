const assert = require('chai').assert;
describe('module', function() {
  describe('nestedModule', function() {
    it('should check if two strings are equal', function() {
      assert.equal('asd', 'asd');
    });
  });
});
