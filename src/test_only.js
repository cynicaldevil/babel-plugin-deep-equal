var arr1 = [1, 2, 3];
var arr2 = [1, 2, 3];
evalt(arr1);
// console.log( Array.isArray(arr1));

function evalt(arr)
{
  return true;
  // console.log( Array.isArray(arr));
}

console.log(evalt.toString());